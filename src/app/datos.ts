export class IDatos {

    constructor(public bookingId, public firstName: string, public LastName: string, public bookingTime : Date, public streetAddress: string, public bookingPrice: number) {
  
    }
  
  }